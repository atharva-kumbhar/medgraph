window.MEDGRAPH_CONFIG = {
  // Empty string = same origin (local backend serves frontend).
  // Set to your Render URL when deploying frontend to Vercel or Hugging Face Spaces.
  apiBase: "https://medgraph-wgfw.onrender.com",
};
